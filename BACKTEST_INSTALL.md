# Backtest — تعليمات الرفع (Railway فقط)

ما يتغيّر شيء في Vercel. كل الإضافة على سيرفر Railway.

## الملفات
داخل مجلد `server/`:
- `signals.js`  ← جديد (مؤشرك مفصول — مشترك بين الماسح والباك تيست)
- `backtest.js` ← جديد (المحرّك + المُحسِّن)
- `backtest.html` ← جديد (صفحة الواجهة)
- `index.js` ← مُعدّل (أضيف: استيراد + أوامر WebSocket + مسار `/backtest`)

> لا توجد مكتبات جديدة — كل شيء يعتمد على ما هو موجود (`node-fetch`) ووحدات Node المدمجة (`zlib`, `fs`, `crypto`).

## الرفع
1. انسخ الملفات الأربعة فوق إلى مجلد `server/` في مستودعك (استبدل `index.js`).
2. ارفع لـ Railway (Git push أو رفع مباشر) ودعه يعيد النشر.
3. افتح: `https://اسم-سيرفرك.up.railway.app/backtest`
4. سجّل الدخول بنفس مستخدم/كلمة مرور التطبيق.

## الاستخدام
1. **البيانات:** حدّد عمق التاريخ (أيام) واضغط «تنزيل». يُنزّل كل العملات × 4 فريمات (5m/30m/1h/4h) مرة واحدة ويُخزّنها مضغوطة. (قد يستغرق وقتاً — الفريمات الصغيرة أثقل.)
2. **باك تيست فردي:** تظهر إعداداتك الحالية تلقائياً؛ عدّلها واضغط «شغّل» لترى العائد/النجاح/الصفقات/أقصى تراجع + منحنى رأس المال.
3. **الفحص التلقائي:** حدّد الميزانية (ساعات) وأقل عدد صفقات واضغط «ابدأ». تشاهد: نسبة الإنجاز + الوقت المنقضي + الوقت المتبقّي + أفضل نتيجة لحظية. عند الانتهاء: أفضل إعداد + لوحة أفضل 10، وتصلك رسالة على تلغرام.

## ملاحظات مهمة
- الباك تيست يعمل على **شموع مغلقة** (بلا repaint) — وهو الأساس الصحيح. نظامك الحي يطلق على الشمعة الحية، لذا لن تتطابق النتائج تماماً حتى نُصلح الحي ليطابق المغلق (الإصلاح الذي ناقشناه).
- أفضل إعداد من الفحص = الأفضل **على الماضي**، لا يضمن المستقبل (overfitting). جرّبه بحذر وعلى رأس مال صغير.
- لتغيير مساحة البحث أو الرسوم: `defaultSpace()` و`FEE_RATE` في `backtest.js`.
- مجلد البيانات الافتراضي: `data/backtest/` (متغيّر `BT_DATA_DIR`). على Railway المجلد مؤقت — للتخزين الدائم اربط Volume.

## للتحقق محلياً (اختياري)
```
node server/backtest.selftest.js   # 15 اختبار للمحرّك
```

## ما الذي يحسّنه الفحص (كل بارامترات الموشر)
- المؤشر: RSI/SMA/EMA + فترة MA + نمط الانعكاس + الدايفرجنس on/off
- الفريم: 5m/30m/1h/4h
- أنواع الإشارة: ob/os/conf كلٌّ on/off
- Trailing RSI: مطفي/شورت/لونق/الاثنين + عتباته (start/gap)
- EMA200 + SuperTrend: مطفي/EMA/ST/الاثنين + فريم الفلتر + فترة/مضاعف ST
- الاتجاه: الكل/لونق/شورت
- الإدارة: TP1 ونسبته، TP2، SL، دخول ثانٍ، تريلينج الخروج، بريك-إيفن، الرافعة
- السيولة: فلتر متوسط حجم التداول (مطفي/10M/50M)

> الحجم الهائل يُعالَج ببحث محدود بميزانيتك الزمنية مع تخزين مؤقت لطبقة الإشارة.
> **مهم:** أُضيف حجم التداول للبيانات (يلزم لفلتر السيولة) — أعد تنزيل البيانات مرة بعد رفع هذه النسخة.